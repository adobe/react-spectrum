# Generated code

@adobe/react-spectrum v3 icon components are generated (`yarn build:icons`) and @react-spectrum/s2 icons are handled through a parcel transformer, not hand-written, and `postinstall` runs `patch-package`, so run install on a fresh clone.
