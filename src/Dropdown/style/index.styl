@import '../../commons'

.coral-Dropdown {
  position: relative;

  .coral-Dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    margin-top: 5px;

    &.align-right {
      left: auto;
      right: 0;
    }
  }
}
