// These styles contain variables and mixins used by all components.
// They should not output any actual styles, to prevent duplicates.
// Common page-level styles live in the `page` folder.

@import '@spectrum/spectrum-abstract-stylus/dist/commons'

@import 'mixins.styl'
@import 'layout.styl'
@import 'layers.styl'
@import '@coralui/coralui-core/styles/utilities/mixins.styl'
@import 'nib'
