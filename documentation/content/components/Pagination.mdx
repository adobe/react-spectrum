import Pagination from '@react/react-spectrum/Pagination';

## Overview

Pagination tracks a current index between 1 and the totalPages. It emits events for progressing by 1 forward or backward.


## Example

<Pagination></Pagination>

```jsx
<Pagination></Pagination>
```

### Variant

#### Button

<Pagination variant="button" mode="primary" totalPages={5} defaultPage={3}></Pagination>

```jsx
<Pagination variant="button" mode="primary" totalPages={5} defaultPage={3}></Pagination>
```

#### Explicit

<Pagination variant="explicit" mode="primary" totalPages={5} defaultPage={3}></Pagination>

```jsx
<Pagination variant="explicit" mode="primary" totalPages={5} defaultPage={3}></Pagination>
```

### Mode

#### CTA

<Pagination variant="button" mode="cta" totalPages={5} defaultPage={3}></Pagination>

```jsx
<Pagination variant="button" mode="cta" totalPages={5} defaultPage={3}></Pagination>
```

#### Primary

<Pagination variant="button" mode="primary" totalPages={5} defaultPage={3}></Pagination>

```jsx
<Pagination variant="button" mode="primary" totalPages={5} defaultPage={3}></Pagination>
```

#### Secondary

<Pagination variant="button" mode="secondary" totalPages={5} defaultPage={3}></Pagination>

```jsx
<Pagination variant="button" mode="secondary" totalPages={5} defaultPage={3}></Pagination>
```



