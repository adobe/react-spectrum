import Avatar from '@react/react-spectrum/Avatar';
import Button from '@react/react-spectrum/Button';
import User from '@react/react-spectrum/Icon/User';


## Overview

An **Avatar** is used to show a user's profile picture or other small avatar.

## Example

### Default

<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" alt="User" />

```jsx
<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" alt="User" />
```

### Disabled

<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" alt="User" disabled />


```jsx
<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" alt="User" disabled />
```

## Internationalization

To internationalize an avatar, a localized string should be passed to the `alt` prop of the avatar.

## Accessibility

An Avatar component renders using an `img` tag. A localized string should be passed to the `alt` prop of the Avatar to provide alternative text to the `img` using the `alt` attribute. If the avatar is used purely for decoration, and alternative text describing the avatar would be redundant, not assigning an `alt` prop will cause `img` to be rendered using an empty `alt=""` attribute.

### Empty `alt` attribute

<Button variant="primary" icon={<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" />}>
   User
</Button>

```jsx
  <Button variant="primary" icon={<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" />}>
     User
  </Button>
```

### Icon Button

  <Button variant="primary" icon={<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" alt="User" />}></Button>

```jsx
  <Button variant="primary" icon={<Avatar src="http://opensource.adobe.com/spectrum-css/2.7.2/docs/img/example-ava.jpg" alt="User" />}></Button>
```
