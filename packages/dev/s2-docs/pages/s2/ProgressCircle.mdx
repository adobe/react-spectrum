import {Layout} from '../../src/Layout';
export default Layout;

import {ProgressCircle} from '@react-spectrum/s2';
import docs from 'docs:@react-spectrum/s2';

# ProgressCircle

<PageDescription>{docs.exports.ProgressCircle.description}</PageDescription>

<VisualExample
  component={ProgressCircle}
  docs={docs.exports.ProgressCircle}
  props={['value', 'isIndeterminate', 'size', 'staticColor']}
  initialProps={{ariaLabel: "Loading…", value: 50}}
  importSource="@react-spectrum/s2"
  type="s2" />

## Value

ProgressCircles are controlled with the `value` prop. By default, the `value` prop represents the current percentage of progress, as the minimum and maximum values default to 0 and 100, respectively.

```tsx render expanded
"use client";
import {ProgressCircle} from '@react-spectrum/s2';

<ProgressCircle aria-label="Loading…" value={25} />
```

Alternatively, a different scale can be used by setting the `minValue` and `maxValue` props.

```tsx render hideImports
"use client";
import {ProgressCircle} from '@react-spectrum/s2';

<ProgressCircle aria-label="Loading…" minValue={50} maxValue={150} value={100} />
```

## Props

<PropTable component={docs.exports.ProgressCircle} links={docs.links} />
