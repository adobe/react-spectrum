import {Layout} from '../../src/Layout';
export default Layout;

import {Switch} from '@react-spectrum/s2';
import docs from 'docs:@react-spectrum/s2';

# Switch

<PageDescription>{docs.exports.Switch.description}</PageDescription>

<VisualExample
  component={Switch}
  docs={docs.exports.Switch}
  props={['size', 'isEmphasized']}
  initialProps={{
    children: 'Low power mode'
  }}
  importSource="@react-spectrum/s2"
  type="s2" />

## Value

Switches are not selected by default. The `defaultSelected` prop can be used to set the default state (uncontrolled). Alternatively, the `isSelected` prop can be used to make the selected state controlled.

```tsx render hideImports
"use client";
import {Switch} from '@react-spectrum/s2';
import {useState} from 'react';

function Example() {
  let [selected, setSelected] = useState(false);

  return (
    <div style={{display: 'flex', flexDirection: 'column', gap: 12}}>
      <Switch defaultSelected>
        Low power mode (uncontrolled)
      </Switch>

      <Switch
        isSelected={selected}
        onChange={setSelected}>
        Low power mode (controlled)
      </Switch>
    </div>
  );
}
```

## Events

Switches accept a user defined `onChange` prop, triggered whenever the Switch is pressed. The example below uses `onChange` to update state and show the current switch status.

```tsx render hideImports
"use client";
import {Switch} from '@react-spectrum/s2';
import {useState} from 'react';

function Example() {
  let [selected, setSelected] = useState(false);

  return (
    <div style={{display: 'flex', flexDirection: 'column', gap: 12}}>
      <Switch onChange={setSelected}>
        Wi-Fi
      </Switch>
      <div>Wi-Fi is: {selected ? 'On' : 'Off'}</div>
    </div>
  );
}
```

## HTML forms

Switch supports the `name` and `value` props for integration with HTML forms.

```tsx render hideImports
"use client";
import {Switch} from '@react-spectrum/s2';

<form>
  <Switch name="power" value="low">Low power mode</Switch>
</form>
```

## Accessibility

In certain cases a visible label isn't needed. For example, a Switch located at the top of a panel which toggles the panel's options on or off. If a visible label isn't specified, an `aria-label` must be provided to the Switch for accessibility.

```tsx render hideImports
"use client";
import {Switch} from '@react-spectrum/s2';

<Switch aria-label="Low power mode" />
```

## Props

<PropTable component={docs.exports.Switch} links={docs.links} />
