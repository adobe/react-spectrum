import {Layout} from '../../src/Layout';
export default Layout;

import {Checkbox} from '@react-spectrum/s2';
import docs from 'docs:@react-spectrum/s2';

# Checkbox

<VisualExample
  component={Checkbox}
  docs={docs.exports.Checkbox}
  props={['size', 'isEmphasized', 'isIndeterminate']}
  initialProps={{children: 'Unsubscribe'}}
  importSource="@react-spectrum/s2"
  type="s2" />

## Value

Checkboxes are not selected by default. The `defaultSelected` prop can be used to set the default state (uncontrolled). Alternatively, the `isSelected` prop can be used to make the selected state controlled.

```tsx render hideImports
"use client";
import {Checkbox} from '@react-spectrum/s2';
import {useState} from 'react';

function Example() {
  let [selected, setSelected] = useState(true);

  return (
    <div style={{display: 'flex', flexDirection: 'column', gap: 12}}>
      <Checkbox defaultSelected>Subscribe (uncontrolled)</Checkbox>
      <Checkbox isSelected={selected} onChange={setSelected}>
        Subscribe (controlled)
      </Checkbox>
    </div>
  );
}
```

## Events

Checkboxes accept a user defined `onChange` prop, triggered whenever the Checkbox is clicked. The example below uses `onChange` to update state and show the current selection status.

```tsx render hideImports
"use client";
import {Checkbox} from '@react-spectrum/s2';
import {useState} from 'react';

function Example() {
  let [selected, setSelected] = useState(false);

  return (
    <div style={{display: 'flex', flexDirection: 'column', gap: 12}}>
      <Checkbox isSelected={selected} onChange={setSelected}>
        Subscribe to updates
      </Checkbox>
      <div>You are {selected ? 'subscribed' : 'unsubscribed'}</div>
    </div>
  );
}
```

## Validation

Checkboxes can display a validation state to communicate to the user if the current value is invalid. Implement your own validation logic in your app and set the `isInvalid` prop accordingly.

```tsx render hideImports
"use client";
import {Checkbox} from '@react-spectrum/s2';

<Checkbox isInvalid>I accept the terms and conditions</Checkbox>
```

## HTML forms

Checkbox supports the `name` and `value` props for integration with HTML forms.

```tsx render hideImports
"use client";
import {Checkbox} from '@react-spectrum/s2';

<form>
  <Checkbox name="newsletter" value="subscribe">
    Subscribe to newsletter
  </Checkbox>
</form>
```

## Props

<PropTable component={docs.exports.Checkbox} links={docs.links} />
