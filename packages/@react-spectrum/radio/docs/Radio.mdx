import {Layout} from '@react-spectrum/docs';
export default Layout;

import docs from 'docs:@react-spectrum/radio';
import packageData from '../package.json';
import {HeaderInfo, PropTable} from '@react-spectrum/docs';

```jsx import
import {Radio} from '@react-spectrum/radio';
import {RadioGroup} from '@react-spectrum/radio';
```

# RadioGroup

<p>{docs.exports.Radio.description}</p>

<HeaderInfo
  packageData={packageData}
  componentNames={['Radio', 'RadioGroup']}
  sourceData={[
  {type: 'Spectrum', url: 'https://spectrum.adobe.com/page/radio-button/'}
  ]} />

## Example

```tsx example
<RadioGroup label="Favorite pet">
 <Radio value="dogs">
   Dogs
 </Radio>
 <Radio value="cats">
   Cats
 </Radio>
</RadioGroup>
```

## Content

Radio buttons can have a visible `label` that can be provided by passing them a child.
A Radio cannot be rendered outside of a RadioGroup.

```tsx example
<RadioGroup label="Size">
 <Radio value="small">
   <i>Small</i>
 </Radio>
 <Radio value="medium">
   <i>Medium</i>
 </Radio>
 <Radio value="large">
   <i>Large</i>
 </Radio>
</RadioGroup>
```

## Value

Only one Radio per RadioGroup can be selected at a time.
An initial, uncontrolled, value can be provided to the RadioGroup using the `defaultValue` prop.
Alternatively, providing a `value` prop to the RadioGroup creates a controlled value.

### Accessibility

For accessibility, all radios within a RadioGroup automatically receive an `aria-checked` attribute
reflecting their individual selection status.

```tsx example
<RadioGroup label="Uncontrolled" defaultValue="dragon">
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

```tsx example
<RadioGroup label="Controlled" value="dragon">
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

## Labeling

When a visible label isn't being used an `aria-label` prop is needed to explicitly set the accessibility name for the RadioGroup.
An `aria-label` prop should not be used for an individual Radio as it is they should always have a visible value.

```tsx example
<RadioGroup aria-label='Favorite Avatar'>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

### Internationalization

To internationalize a Radio, a localized string should be set as the child content of the Radio.
For languages that are read right to left (e.g. Hebrew and Arabic), the Radio is placed on the right side of the text.

## Events

Radio buttons accept a user defined `onChange` prop, triggered whenever the Radio is clicked.
The example below uses `onChange` to log how the user is interacting with the component.

```tsx example
function Example() {
  let [selected, setSelection] = React.useState('none');

  return (
    <div>
      <div>You have selected: {selected}</div>
      <RadioGroup aria-label="Favorite Avatar" onChange={value => setSelection(value)}>
       <Radio value="wizard">
         Wizard
       </Radio>
       <Radio value="dragon">
         Dragon
       </Radio>
      </RadioGroup>
    </div>
  );
}
```

## Validation

RadioGroups can display a validation state to communicate to the user if the current value is invalid.
Implement your own validation logic in your app and pass `"invalid"` to the RadioGroup via the `validationState` prop.

```tsx example
<RadioGroup label="Example Options" validationState='invalid'>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

## Props

<PropTable component={docs.exports.Radio} links={docs.links} />

## Visual Options

### Orientation

A RadioGroup is vertically aligned by default.
An optional prop can be passed to the RadioGroup to position the radio buttons inline.

```tsx example
<RadioGroup label="Example Options" orientation='horizontal'>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

### Label Position

The default position of a label is on top of the RadioGroup.
An optional prop can be passed to the RadioGroup that will position the label at the start of the topmost Radio.

```tsx example
<RadioGroup label="Example Options" labelPosition='side'>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

### Label Align

Labels can be aligned to the "start" or the "end" of the RadioGroup.
For left-to-right (LTR) languages, "start" refers to the left most edge of the RadioGroup
and "end" refers to the right most edge. For right-to-left (RTL) languages, this is flipped.

```tsx example
<RadioGroup label="Example Options" labelAlign='end'>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

### Disabled

The disabled prop applies a disabled style as well as preventing click events and
keyboard navigation from performing selection.
This prop can be passed to the entire RadioGroup or individual Radio buttons.
[View Guidelines](https://spectrum.adobe.com/page/radio-button/#Disabled)

```tsx example
<RadioGroup label="Example Options" isDisabled>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

```tsx example
<RadioGroup label="Example Options">
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon" isDisabled>
   Dragon
 </Radio>
</RadioGroup>
```

### Read Only

In this mode no visual indication is given to the user that any option within the RadioGroup has been selected.
This prop can be passed to the entire RadioGroup but not to individual Radio buttons.

```tsx example
<RadioGroup label="Example Options" isReadOnly>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```

### Emphasized

By default, Radio buttons are not emphasized (gray filling).
The emphasized style provides a blue filling for visual prominence.
[View Guidelines](https://spectrum.adobe.com/page/radio-button/#Emphasis)

```tsx example
<RadioGroup label="Example Options" isEmphasized>
 <Radio value="wizard">
   Wizard
 </Radio>
 <Radio value="dragon">
   Dragon
 </Radio>
</RadioGroup>
```
